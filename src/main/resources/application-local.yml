spring:
  kafka:
    consumer:
      bootstrap-servers:
      - localhost:9093
      properties:
        schema.registry.url: http://localhost:8083

    producer:
      bootstrap-servers:
      - localhost:9094
      properties:
        schema.registry.url: http://localhost:8084
